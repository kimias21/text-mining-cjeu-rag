# --- Ingestion (Step 1) ---
beautifulsoup4
lxml

# --- Embeddings & vector DB (Step 2) ---
faiss-cpu
sentence-transformers
numpy

# --- Agents / LLM orchestration (Steps 3-4) ---
google-genai       # Gemini API -- free tier, no credit card required
tiktoken

# --- Chat interface (Step 5) ---
streamlit

# --- Evaluation dashboard (Step 6) ---
pandas
openpyxl          # for the results_exam_june_2026 Excel export
ragas             # if using the official RAGAS library rather than reimplementing metrics

# --- Bonus: Knowledge Graph ---
networkx           # in-memory graph (chosen over Neo4j -- see src/knowledge_graph/README.md)
# neo4j             # uncomment if using a dedicated graph database instead
