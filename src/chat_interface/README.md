# Chat interface & logging (Step 5)

User-facing chat app + structured JSON conversation logs (question, answer, retrieved docs
with case number/domain/area/instrument/source) feeding the evaluation dashboard.
