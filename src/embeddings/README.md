# Embeddings & Vector DB (Step 2)

Chunking strategy, embedding model wrapper, and FAISS index construction over
data/json/{environmental,agricultural}. One index per thematic-area/domain slice
if going the multi-agent specialization route.
